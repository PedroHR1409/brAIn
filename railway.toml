[build]
builder = "NIXPACKS"
buildCommand = "npm install --include=dev && cd apps/server && npx tsdown"

[deploy]
startCommand = "node apps/server/dist/index.mjs"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
